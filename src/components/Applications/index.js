import React from "react";
import { Container } from "react-grid-system";

export default () => {
    return (
        <section className="section section-applications">
            <Container className="container" fluid>
                
            </Container>
        </section>
    );
};
